import React from 'react';
import ReactDOM from 'react-dom';

import App from './components/App';

// render app component to DOM element
ReactDOM.render(<App />, document.querySelector('.container'));
